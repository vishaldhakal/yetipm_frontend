/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "127.0.0.1",
      "via.placeholder.com",
      "maps.googleapis.com",
      "maps.gstatic.com",
      "dashboard.sixdesign.ca",
    ],
  },
};

export default nextConfig;
